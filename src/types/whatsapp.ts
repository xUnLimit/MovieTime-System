// ===========================
// WHATSAPP TYPES
// ===========================

export interface WhatsAppData {
  cliente: string;
  categoria: string;
  correo: string;
  contrasena: string;
  vencimiento: string;
  monto: string;
  diasRetraso?: number;
}
