// ===========================
// WHATSAPP TYPES
// ===========================

export interface WhatsAppData {
  cliente: string;
  servicio: string;
  categoria: string;
  perfilNombre: string;
  correo: string;
  contrasena: string;
  vencimiento: string;
  monto: string;
  codigo: string;
  items: string;
  diasRetraso?: number;
}
